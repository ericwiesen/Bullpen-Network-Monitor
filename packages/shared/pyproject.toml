[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "monitoring-shared"
version = "0.1.0"
description = "Shared models and DB for monitoring service"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*"]
